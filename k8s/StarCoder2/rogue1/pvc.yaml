apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ollama-pvc-rogue1
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-storage
  volumeName: ollama-pv-rogue1
  resources:
    requests:
      storage: 100Gi
