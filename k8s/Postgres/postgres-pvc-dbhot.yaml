apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-pvc-dbhot
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-storage
  volumeName: postgres-pv-dbhot
  resources:
    requests:
      storage: 50Gi
